import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        backgroundColor: "#FFFFFF",
        padding: 14,
        borderRadius: 12,
        marginBottom: 20,
        borderWidth: 1,
        borderColor: "#E2E8F0",
    },
});