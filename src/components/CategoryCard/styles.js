import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    card: {
        backgroundColor: "#7C3AED",
        padding: 16,
        borderRadius: 12,
        minWidth: 120,
        alignItems: "center",
    },

    title: {
        color: "#FFFFFF",
        fontWeight: "bold",
        fontSize: 16,
    },
});