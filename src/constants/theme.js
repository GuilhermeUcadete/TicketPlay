import colors from "./colors";

const theme = {
    colors,

    spacing: {
        xs: 4,
        sm: 8,
        md: 16,
        lg: 24,
        xl: 32,
    },

    borderRadius: {
        sm: 8,
        md: 12,
        lg: 16,
    },
};

export default theme;